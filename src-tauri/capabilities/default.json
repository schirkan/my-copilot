{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window (skeleton — wird in späteren Karten erweitert)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}