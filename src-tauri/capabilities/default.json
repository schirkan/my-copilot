{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. The Copilot CLI is spawned by the github-copilot-sdk (bundled-cli feature) directly; no shell-plugin sidecar entry is needed.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}